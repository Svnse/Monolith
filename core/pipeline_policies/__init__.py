"""Pipeline policy modules — each declares register_with(pipeline).

Every .py file in this directory must be declared in
core/pipeline_registry.py:POLICIES. The boot-time validator enforces this.
"""
